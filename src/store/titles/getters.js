const titlesErrors = state => state.titlesErrors
const titles = state => state.titles
const pagination = state => state.pagination
const currentTitle = state => state.currentRelease
const currentStep = state => state.step

export {
  titlesErrors,
  titles,
  pagination,
  currentTitle,
  currentStep
}
